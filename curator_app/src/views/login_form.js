'use strict'
const m = require('mithril')
const LoginForm = {
  view (vnode) {
    return [
      m('.header.text-center.mb-2',
        m('h4', 'Login Form Placeholder'))
    ]
  }
}
module.exports = LoginForm