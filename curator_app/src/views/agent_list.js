'use strict'
const m = require('mithril')
const AgentList = {
  view (vnode) {
    return [
      m('.header.text-center.mb-2',
        m('h4', 'Agent List Placeholder'))
    ]
  }
}
module.exports = AgentList